urls:
  gitlab: https://gitlab.onefiserv.net/
framework:
  playwright:
    launch-options:
      # Supported values are "chrome", "chrome-beta", "chrome-dev", "chrome-canary", "msedge", "msedge-beta", "msedge-dev", "msedge-canary"
      channel: chrome
      # headless: true
      # slowMo: 100
      # timeout: 30000
      # args:
      #   - --ignore-certificate-errors
      #   - --proxy-server=http://127.0.0.1:8080
      # chromiumSandbox: false
      # devtools: false
      # downloadsPath: my/download/path
      # env:
      #   example-key1: example-value1
      #   example-key2: example-value2
      # executablePath: my/executable/path
      # firefoxUserPrefs:
      #   example-key1: example-value1
      #   example-key2: example-value2
      # handleSIGHUP: true
      # handleSIGINT: true
      # handleSIGTERM: true
      # ignoreAllDefaultArgs: false
      # ignoreDefaultArgs: false
      # proxy:
      #   server: my-server
      #   bypass: my-bypass
      #   username: my-username
      #   password: my-password
      # tracesDir: my/traces/dir
